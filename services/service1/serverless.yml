service: serverless-lerna-yarn-service1

plugins:
  - serverless-bundle

provider:
  name: aws
  runtime: nodejs14.x
  region: us-east-1
  stage: dev

functions:
  get:
    handler: handler.main
    events:
      - http: GET /

